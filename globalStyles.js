import { StyleSheet } from 'react-native';

module.exports = StyleSheet.create({
    primaryText: {
        fontSize: 24,
        color: '#ECE9E9',
    },

    container: {
        flex: 1,
        alignItems: 'center',
    },
});